spring.application.name: cb-service

server:
  port : 8181

postgres:
  hikari:
    minimumIdle: 3
    maximum-pool-size: 15
    auto-commit: false
    username: "postgres"
    password: "root"
    driver-class-name: org.postgresql.Driver
    jdbc-url: jdbc:postgresql://localhost:5532/cb_db

cbr:
  wsdl: https://cbr.ru/DailyInfoWebServ/DailyInfo.asmx?WSDL
  connection: 10000
  read: 10000


